{
    "fieldName": "pack.burgs",
    "noteName": "@{return `${this.state}-${(this.state > 0) && dataRoot.pack.states.find(state => state.i === this.state)?.name || "Unknown" }/${dataRoot.pack.cells.find(c => c.i === this.cell)?.province}-${dataRoot.pack.provinces.find(p => p.i === dataRoot.pack.cells.find(c => c.i === this.cell)?.province)?.fullName}/${this.name}`}",
    "folderName": "01-Campaigns/Test Campaign Ladonia/05-Atlas/States",
    "namePrefix": "",
    "nameSuffix": "",
    "batchStep": "Burgs"
}